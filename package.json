{
  "name": "ledger",
  "private": true,
  "version": "1.0.0",
  "description": "Personal finance tracker — what did I spend today, and where did the money go?",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server/server.js",
    "dev": "node --watch server/server.js"
  },
  "dependencies": {
    "@vly-ai/integrations": "^0.6.14",
    "better-sqlite3": "^12.4.1",
    "dotenv": "^17.2.3",
    "express": "^5.1.0"
  }
}
